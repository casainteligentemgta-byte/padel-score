export default function TestPage() {
    return (
        <div style={{ minHeight: '100vh', background: '#0a0a0a', color: '#fff', padding: 24, fontSize: 24 }}>
            Si ves esto, el servidor y el layout funcionan.
        </div>
    );
}
