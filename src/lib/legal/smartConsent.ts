import { CURRENT_TERMS_VERSION } from '@/lib/legal/termsVersion';

export const SMART_CONSENT_STATUS_ACCEPTED = 'accepted';

/** Alineada con la versión canónica (registro + inscripción a torneos). */
export const SMART_CONSENT_LEGAL_VERSION = CURRENT_TERMS_VERSION;

